---
title:  "Ruby/JRuby"
doc_title: "Using Ruby/JRuby on Snap CI"
---

Snap uses [rbenv](https://github.com/sstephenson/rbenv) to install and manage rubies. The following versions are currently available. You may install additional gems using `gem install GEM_NAME --no-ri --no-rdoc`.

**Ruby**

<%= monospaced_array_to_sentence_string(data.aurora_versions.ruby) %>

**JRuby**

<%= monospaced_array_to_sentence_string(data.aurora_versions.jruby) %>

We also pre-install the following gems that users may find useful:

* bundler - `<%= data.aurora_versions.bundler %>`
* rake - `<%= data.aurora_versions.rake %>`

