---
title:  Pipelines
doc_title:  Snap CI Pipelines API
---

# Get the build history (all pipelines)

Returns a list of most recent pipelines. This list is paginated and will contain a link to the `next` or `previous` page of pipelines as the case may be.

`GET /project/:owner/:repository/branch/:branch_name/pipelines`{:.api-method-url}

*For Pull request:*

`GET /project/:owner/:repository/pull/:pull_request_counter/pipelines`

<%= render_api_example 'pipelines/get-all-pipelines',
      method: :get,
      url:    'https://api.snap-ci.com/project/snap-ci/api-docs-sample-repository/branch/master/pipelines'
%>


# Get a pipeline

Returns a pipeline. Contains relevant links to fetch the `next` or `previous` if there is one.

`GET /project/:owner/:repository/branch/:branch_name/pipelines/:pipeline_counter`{:.api-method-url}

<%= render_api_example 'pipelines/get-a-pipeline',
      method: :get,
      url:    'https://api.snap-ci.com/project/snap-ci/api-docs-sample-repository/branch/master/pipelines/25'
%>

# Get latest pipeline

Redirects to the latest pipeline.

`GET /project/:owner/:repository/branch/:branch_name/pipelines/{latest,newest}`{:.api-method-url}

<%= render_api_example 'pipelines/get-latest-pipeline',
      method: :get,
      url:    'https://api.snap-ci.com/project/snap-ci/api-docs-sample-repository/branch/master/pipelines/latest'
%>

# Get oldest pipeline

Redirects to the oldest pipeline.

`GET /project/:owner/:repository/branch/:branch_name/pipelines/{oldest,earliest}`{:.api-method-url}

<%= render_api_example 'pipelines/get-oldest-pipeline',
      method: :get,
      url:    'https://api.snap-ci.com/project/snap-ci/api-docs-sample-repository/branch/master/pipelines/oldest'
%>
